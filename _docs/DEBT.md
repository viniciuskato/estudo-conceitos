# Dívida Técnica — Estudo de Conceitos

Arquivo rastreado pelo Git. Toda inconsistência identificada mas não corrigida na sessão deve ser registrada aqui — nunca deixar silenciosa.

**Formato de entrada:**
```
- [ ] `caminho/arquivo.html` — descrição do problema (YYYY-MM-DD)
```

Ao corrigir: marcar `[x]` e mover para a seção **Resolvida** com data de resolução.

---

## Pendente

- [ ] `_docs/skills-export/mecanismo/SKILL.md` — atualizado nesta sessão (novo item 4b + checagem mecânica (f) contra shell desatualizado) mas empacotamento (.skill) e reinstalação local (Settings > Capabilities > Skills) ainda pendentes — usuário precisa rodar `empacotar_skills.py` localmente. (2026-08-07)
- [ ] **Cross-links entre compêndios-irmãos de `compêndios/medicina/mecanismos/imunologia/` (12 dos 13 arquivos pré-existentes + `index.html`) usam o caminho pré-migração `../../../../medicina/imunologia/arquivo.html`, que não resolve para nada desde a migração de 2026-07-10 (pasta real é `compêndios/medicina/mecanismos/imunologia/`, sem esse prefixo).** Confirmado em `resposta-a-patogenos.html` (dep-panel inteiro) e em todos os 12 `href` de módulo de `index.html` — script de migração de 2026-07-10 moveu os arquivos mas não corrigiu os links internos entre eles. Não corrigido nesta sessão (fora de escopo, ~13 arquivos) — só o arquivo novo (`resposta-imune-bacterias-extra-intracelulares.html`) usa o caminho correto (same-directory). Corrigir em lote (find/replace `../../../../medicina/imunologia/` → `` vazio, dentro da própria pasta) na próxima sessão que tocar este domínio, ou via `raio-x-cowork` se o usuário preferir auditoria completa primeiro. (2026-08-07)
- [ ] `compêndios/medicina/mecanismos/imunologia/resposta-imune-bacterias-extra-intracelulares.html` — verificação visual no Chrome PENDENTE (verificação bash-equivalente via Grep/Read OK: fecha `</body></html>`, 718 linhas, sem `const const`/`var var`/artefatos reais — únicos matches de "TODO" foram falsos positivos em "todo"/"toda", 136 acentos PT-BR, 18 seções = 18 links de sidebar, 10 `plate-block` com `img` local verificado por checksum + 1 imagem externa PMC). Lembrete: `git add` das 10 imagens locais em `img/resposta-imune-bacterias-extra-intracelulares/` + `push.bat` na raiz do repositório — pendente de execução pelo usuário. (2026-08-07)
- [ ] `compêndios/medicina/mecanismos/imunologia/resposta-imune-bacterias-extra-intracelulares.html` — dep-panels de `resposta-a-patogenos.html`, `sistema-complemento.html` e `respostas-th1-th2-th17.html` ("Materiais que dependem deste") ainda não listam este novo arquivo como dependente — corrigir na próxima sessão que reabrir qualquer um desses três, mesmo padrão bidirecional já usado em cardiologia. (2026-08-07)
- [ ] `compêndios/medicina/mecanismos/fisiologia/avaliacao-funcao-renal.html` — verificação visual no Chrome PENDENTE (verificação bash-equivalente via Grep/Read OK — ver `_docs/project_estado.md`, entrada 2026-08-07). (2026-08-07)
- [ ] `compêndios/medicina/mecanismos/farmacologia/hipertensao_sraa_anti-hipertensivos.html` — cross-link bidirecional pendente com `../fisiologia/avaliacao-funcao-renal.html` (ajuste de dose de IECA/BRA por função renal). Este arquivo é de template legado (sem `dep-panel`, sidebar fixa sem toggle — já registrado como divergência antiga neste DEBT.md) — não retroagir a estrutura só por causa deste link; se o arquivo for reaberto por outro motivo, adicionar o cross-link no corpo do texto ou no dep-panel quando ele for criado. (2026-08-07)
- [ ] **Busca de imagem como passo padrão da criação (não pendência adiável) — rollout de Casos Clínicos para Compêndios/Provas/Sistemas ainda não avaliado.** Regra promovida em `casos-clinicos/CLAUDE.md` (2026-08-07): ao criar material novo, buscar ativamente ao menos uma imagem pertinente antes de considerar pronto, em vez de registrar "sem imagem" como pendência de sessão futura. Mesmo padrão de rollout gradual já usado para outras convenções cross-domínio (ex. clicabilidade de `.evid`, "fontes fortes") — avaliar extensão às demais skills de `Base de estudos` na próxima vez que uma delas criar material novo sem imagem, não retrofit em massa agora. Origem: usuário questionou ausência de imagem no 2º caso de Casos Clínicos, 2026-08-07.

- [ ] `compêndios/medicina/cardiologia-semiologia.html` — legenda da Fig. 1 (`plate-caption`) tem `<b>Fig. 1 — Focos de ausculta<\b>` (barra invertida em vez de `</b>`), tag de fechamento malformada. Não quebra a renderização visível (navegadores ignoram silenciosamente), mas é markup inválido — corrigir para `</b>` na próxima vez que o arquivo for reaberto. Achado ao investigar uma correção do usuário em `casos-clinicos/endocardite-infecciosa-avc-embolico.html`, que reaproveitava a mesma figura (usuário apontou que a legenda tratava os 5 pontos coloridos como "5 focos" quando só 4 são clássicos — o corpo de texto deste arquivo já faz a distinção correta "4 focos + 1 ponto acessório de Erb", só a legenda da imagem em si é que listava os 5 sem essa ressalva; corrigido no arquivo de caso, não retroaplicado aqui). (2026-08-06)

- [ ] `compêndios/medicina/mecanismos/semiologia/sindromes-bronco-pleuro-pulmonares.html` — verificação visual no Chrome PENDENTE (Claude-in-Chrome não navega de `chrome://newtab` para `file://` neste ambiente, mesma limitação já registrada para `inteligencia-artificial.html`). Verificação bash-equivalente via Grep/Read OK após adição das 4 pranchas: fecha `</body></html>`, 619 linhas, sem const const/var var/artefatos reais, 815 acentos PT-BR, 16 seções = 16 links de sidebar, 4 `plate-block`/4 `data-caption`/4 `id="fig-N"`. Cada uma das 4 imagens foi individualmente confirmada carregando (screenshot da URL direta do Wikimedia) antes de referenciada no HTML — risco residual é só de layout/CSS, já visualmente validado em `cardiologia-semiologia.html` (mesmo CSS de `plate-block`/lightbox, byte-idêntico). (2026-08-06)
- [ ] `compêndios/medicina/cardiologia-semiologia.html`, `compêndios/medicina/mecanismos/fisiopatologia/doencas-circulatorias.html`, `compêndios/medicina/infectologia-fundamentos.html` — dep-panels ("Materiais que dependem deste") ainda não listam `sindromes-bronco-pleuro-pulmonares.html` como novo compêndio que faz cross-link para eles — corrigir na próxima sessão que reabrir qualquer um desses arquivos, seguindo o padrão bidirecional já usado entre cardiologia-anatomia/cardiologia-semiologia. (2026-08-06)
- [ ] `_docs/skills-export/mecanismo-medicina-clinica/SKILL.md` — atualizado nesta sessão (nova subárea Semiologia + cross-link prioritário) mas empacotamento (.skill) e reinstalação local (Settings > Capabilities > Skills) ainda pendentes — usuário precisa rodar `empacotar_skills.py` localmente. (2026-08-06)
- [ ] `compêndios/medicina/infectologia-fundamentos.html` — verificação visual no Chrome via surge PENDENTE (verificação bash-equivalente via Grep/Read OK: fecha `</body></html>`, 548 linhas, sem const const/var var/artefatos reais, 108 acentos PT-BR, 19 seções = 19 links de sidebar). (2026-08-05)
- [ ] `compêndios/medicina/infectologia-fundamentos.html` — sem pranchas de imagem: conteúdo majoritariamente conceitual/laboratorial (fases da infecção, síndromes, hemograma), sem correspondente anatômico direto — mesmo padrão de ausência deliberada de `medicina.html`/`medicina-familia-comunidade.html`. Único candidato plausível é a linha do tempo "Contágio → Incubação → Pródromos → Doença/Estado → Convalescença" (presente na aula de origem como diagrama, hoje representada só como tabela de texto neste compêndio) — reavaliar recriar como imagem/SVG próprio (não depende de banco CC externo) numa sessão futura, se o usuário quiser. (2026-08-05)
- [ ] `compêndios/medicina/hematologia-hemograma-anemias.html` — dep-panel ("Materiais que dependem deste") ainda não lista `infectologia-fundamentos.html` como novo compêndio que faz cross-link para sua seção de leucograma — corrigir na próxima sessão que reabrir este arquivo, seguindo o padrão bidirecional já usado entre cardiologia-anatomia/cardiologia-semiologia. (2026-08-05)
- [ ] `compêndios/medicina/medicina-familia-comunidade.html` — verificação visual no Chrome via surge PENDENTE (verificação bash-equivalente via Grep/Read OK: fecha `</body></html>`, 508 linhas, sem TODO/FIXME/placeholder/rascunho/debug, 112 acentos PT-BR, 17 seções = 17 links de sidebar). (2026-08-05)
- [ ] `compêndios/medicina/medicina-familia-comunidade.html` — sem pranchas de imagem: conteúdo majoritariamente histórico-conceitual (Flexner, determinantes sociais, RCOP/SOAP) sem correspondente anatômico direto, mesmo padrão de ausência deliberada já aplicado em `medicina.html`/`fisica.html`. Único candidato plausível é o diagrama de determinação social da saúde de Dahlgren-Whitehead (presente na aula de origem como imagem) — reavaliar buscar fonte CC equivalente no Wikimedia numa sessão futura, se o usuário quiser pranchas neste compêndio. (2026-08-05)

- [ ] `compêndios/medicina/hematologia-hemograma-anemias.html` — sem pranchas de imagem (não buscadas nesta sessão de criação). Candidatos plausíveis para sessão futura: diagrama de hematopoiese (linhagens mieloide/linfoide), esfregaço de sangue periférico com hemácias normais vs. microcíticas/macrocíticas, morfologia de esferócitos/esquizócitos/drepanócitos. Verificar hierarquia de fontes (OpenStax A&P tem pranchas de hematopoiese e tipos celulares) antes de assumir ausência de imagem CC adequada — diferente de física/filosofia, hematologia tem correspondente morfológico real, então a busca vale a pena numa próxima sessão. (2026-08-05)
- [ ] `compêndios/medicina/hematologia-hemograma-anemias.html` — verificação visual no Chrome via surge PENDENTE (verificação bash-equivalente via Grep/Read OK: fecha `</body></html>`, 663 linhas, sem `const const`/artefatos/TODO/placeholder, 158 acentos PT-BR, 24 seções = 24 links de sidebar). (2026-08-05)
- [ ] `index.html` (catálogo raiz) — `compêndios/medicina/cardiologia-semiologia.html` (criado em 2026-08-05, sessão anterior à de hematologia) nunca foi adicionado ao catálogo, nas duas seções onde cardiologia-anatomia.html aparece (listagem geral ~linha 184, tab-atlas ~linha 417) — gap pré-existente encontrado incidentalmente ao adicionar o card de hematologia-hemograma-anemias.html nesta sessão, não corrigido aqui por estar fora do escopo desta tarefa. Corrigir replicando o padrão do card de hematologia adicionado nesta sessão. (2026-08-05)

- [ ] **Retrofit de largura (.prose 1000/1180px) e modo claro/escuro ao acervo existente de Compêndios/Sistemas/Provas — decisão em aberto, não retroagir por padrão.** Novo padrão de `template-v2-spec.md` (2026-08-05) aplicado só a `cardiologia-semiologia.html`/`cardiologia-anatomia.html` nesta sessão; os demais ~23 HTMLs ativos (imunologia, fisiologia, farmacologia, fisiopatologia, microbiologia, IA, filosofia, física, investimentos-compêndio) continuam com `.prose` em `ch` e sem toggle de tema. Retroaplicar só se um arquivo for reaberto por outro motivo e o usuário confirmar, ou se pedido explícito de retrofit em lote. (2026-08-05)

- [ ] **Unificação de arquivos de estado/mapa (compêndios × mecanismo) — ainda não feita.** `Base de estudos/compêndios/CLAUDE.md` (linha 23) afirma que esta pendência já estava "registrada em `_docs/DEBT.md`", mas não estava — item adicionado agora para a referência cruzada ficar correta. Desde a migração de mecanismo de medicina para `compêndios/medicina/mecanismos/` (2026-07-10), o histórico/pendências de mecanismo continua vivendo separado (`project_estado.md`, `project_mapa_materiais.md`) do histórico/pendências de compêndio de área (`compêndios/compendio_estado.txt`, `compêndios/compendio_mapa.txt`) — mesmo domínio Cowork, dois pares de arquivo de estado não unificados. Não decidido ainda: unificar em um par único, ou manter dois pares com nomenclatura clara de escopo (área vs. mecanismo). (2026-07-15, registrado retroativamente — decisão original de não unificar foi em 2026-07-10)

- [ ] `_acervo/produtos/` (ex-`automoveis/`) — renomeado em 2026-07-09 de "automóveis" para "produtos" a pedido do usuário, com duas ramificações (`estudo/` para produto único, `comparativo/` para comparação entre produtos concorrentes) que se intercambiam. Hoje só tem exemplos de carro. Não é candidato a domínio próprio (fora de `_acervo/`) enquanto for só uma categoria de produto — reavaliar promoção só se o padrão se repetir com outra categoria (eletrônicos, eletrodomésticos etc.), mesmo critério de maturidade usado para a decisão de Línguas. Ver `_acervo/LEIA-ME.md`, seção "produtos/". (2026-07-09)

- [ ] `_acervo/farmacologia/Material_Didatico_Farmacologia.html` — movido em 2026-07-09 (auditoria raio-x-cowork) de `Dados/Medicina/Estudos próprios/` para dentro do Cowork, por não ser dado bruto (é material didático já elaborado, formato pré-modelo). Cobre anti-inflamatórios, anti-histamínicos e antibacterianos — confirmado, por comparação de títulos/seções, que **não** se sobrepõe aos três compêndios de farmacologia já ativos (antiagregantes/anticoagulantes/trombolíticos, antifúngicos, hipertensão/SRAA). É gap real de conteúdo. Pendência: adaptar ao `template-v2-spec.md` e à convenção de `mecanismo-medicina-clinica` (provavelmente 3 arquivos novos em `medicina/farmacologia/`) — não é trabalho estrutural, é primeira reescrita substancial de conteúdo; por regra de pré-geração já registrada neste arquivo, considerar sessão em Opus + Esforço Alto/Extra. Decisão de quando retomar é do usuário. Ver `_acervo/LEIA-ME.md`, seção "farmacologia/". (2026-07-09)

- [ ] `provas/medicina/agressao-defesa.html`, `epidemiologia.html`, `pesquisa-em-saude.html`, `semiologia.html` — migrados em 2026-07-03 do projeto Cowork "Estudo para prova". Migração foi cópia mecânica preservando o template de origem — os 4 arquivos NÃO seguem `_docs/template-v2-spec.md` (CSS/sidebar/tooltips diferentes do padrão desta base). Alinhar ao spec canônico é reescrita de conteúdo significativa, não trabalho estrutural — recomendação (não requisito) é fazer essa reescrita numa sessão em Opus + Esforço Alto/Extra, dado o volume de conteúdo a recalibrar; a decisão de modelo é do usuário, pode ser feito em Sonnet se preferir. Caches de conteúdo (`_cache/agressao-defesa-conteudo.md`, `_cache/epidemiologia-conteudo.md`) também migrados; `pesquisa-em-saude` e `semiologia` não tinham cache no projeto de origem. (2026-07-03)
- [ ] Projeto Cowork "Tutorial" tem overlap parcial com `mecanismo` (hoje subskill-folha de `compendio`, não skill-pai própria — ver desmembramento de 2026-07-10) — mesma filosofia didática, fluxo de sessão diferente (sessão socrática ligada a documento de abertura pontual, não compêndio de referência permanente) — sinalizado pelo usuário para avaliação futura, ainda não decidido. (2026-07-03; terminologia atualizada em 2026-07-10)
(item resolvido — ver seção Resolvida: "Projeto Cowork 'Línguas' — status de domínio")
- [ ] `compêndios/medicina/medicina.html` — verificação visual no Chrome via surge PENDENTE (verificação bash-equivalente via Grep/Read OK, reconfirmada em 2026-07-03 após a adição do painel de dependências: fecha `</body></html>`, sem `const const`, sem artefatos reais, 21 ocorrências de classes `dep-*`). Compêndio panorâmico de TODA a medicina (nível compendio-medicina), EM INGLÊS por regra idioma=fontes. Subdomínio surge já mapeado nas instruções: `compendios-medicina.surge.sh` → `compêndios/medicina/`, abrir `/medicina.html`. (2026-07-02; reaberto e editado em 2026-07-03 — ver entrada de auditoria abaixo)
- [ ] `compêndios/fisica/fisica.html` — mesmo achado do item de auditoria abaixo: não tem Painel de dependências (`dep-panel`), obrigatório pelo `template-v2-spec.md`. Ao ser auditado em 2026-07-03, o usuário optou por corrigir só `medicina.html` nesta sessão; `fisica.html` fica como decisão em aberto — corrigir se o arquivo for reaberto por outro motivo e o usuário confirmar, não retroaplicar por padrão. (2026-07-03)
- [ ] `compêndios/fisica/fisica.html` — ainda sem Painel de dependências. Agora é pendência concreta (não mais "decisão em aberto"): `compendio/SKILL.md` foi corrigido em 2026-07-03 (ver Resolvida) para exigir o painel também no arquivo-pai, então `fisica.html` diverge do próprio checklist da skill. Corrigir seguindo o padrão já aplicado em `medicina.html` (colunas "Este compêndio usa"/"Leia antes se necessário" vazias/com nota, "Materiais que dependem deste" com os cross-links já presentes no corpo). (2026-07-03)
- [ ] `compêndios/medicina/medicina.html` — sem pranchas anatômicas (plate-block): é um mapa de campo panorâmico (prosa conceitual sobre a estrutura da disciplina, não anatomia de um órgão específico), com 2 recursos visuais tipo `.subarea-grid` (10 cards do mapa) + caixas `.kbox`/`.box`/`.hist`/`.controversia`. Pranchas Gray's/OpenStax cabem melhor nos compêndios de subárea (ex.: cardiologia-anatomia já as tem), não no arquivo-pai. Ausência deliberada conforme regra de imagens. Reavaliar só se surgir figura panorâmica CC adequada (ex.: diagrama das camadas básica/clínica/população). (2026-07-02)

- [ ] `compêndios/fisica/fisica.html` — verificação visual no Chrome via surge PENDENTE (verificação bash OK, confirmada via Read direto: 684 linhas, fecha `</body></html>`, sem char de substituição `\xef\xbf\xbd` no conteúdo EN, sem artefatos, 21 seções = 21 links, 14 tooltips `.term`, 21 `.sl-pt`). Nota: `wc -l`/`tail` do bash reportaram 659 linhas truncadas (cache OneDrive) — integridade confirmada por Read. Subdomínio surge ainda não configurado para `compêndios/fisica/`. (2026-07-02)
- [ ] `compêndios/fisica/fisica.html` — sem pranchas (plate-block): conteúdo é formalismo matemático + diagramas conceituais sem correspondente adequado em Gray's/OpenStax (fontes anatômicas). Ausência deliberada conforme regra de imagens em instrucoes-projeto.txt ("pular a prancha... não forçar imagem inadequada"). Reavaliar se surgir banco de figuras de física CC apropriado (ex.: diagramas de fenda dupla, espectro EM, curva de rotação galáctica). Recursos visuais atuais: 2 `.data-table` (4 forças fundamentais; composição do universo) + classe `.eq` para equações centrais. (2026-07-02)
- [ ] **Idioma do acervo — legado grandfathered (NÃO retroagir por padrão):** a regra "idioma = fontes primárias" (template-v2-spec.md) passou a ser seguida em 2026-07-02 (primeiro material sob ela: `fisica.html`, reescrito de PT para inglês por ter fontes anglófonas). Os 25 HTMLs ativos anteriores em `pt-BR` cujas fontes primárias são majoritariamente anglófonas (imunologia ×?, fisiologia, farmacologia, fisiopatologia, microbiologia, IA, filosofia/epistemologia — investimentos tem base BR mista) tecnicamente divergem da regra. Decisão: tratar como legado, NÃO converter em lote (ver "Escopo de melhorias — não retroagir"). Converter um arquivo só se reaberto por outro motivo E o usuário pedir. Item mantido como decisão em aberto, não pendência a resolver por padrão. (2026-07-02)

- [x] `_docs/template-v2-spec.md` — **RESOLVIDO (2026-07-01, sessão epistemologia)**: a suposta truncagem no final ("Leituras recomendadas") NÃO existe no arquivo em disco. Ao abrir via Read direto (ferramenta que lê o arquivo real do Windows, não o cache do sandbox), o arquivo tem 366 linhas e termina íntegro — a seção "Leituras recomendadas" está completa, com as três camadas obrigatórias (Ponto de entrada → Aprofundamento → Consulta especializada) e o parágrafo final sobre anotação. O que aparecia cortado em 359 linhas via `tail`/`git show` no bash era o **cache do OneDrive/sandbox** já documentado neste projeto (afeta também `git show`). Confirmação cruzada: `git status` não lista o arquivo como modificado (logo disco == HEAD do ponto de vista do Git real do Windows). Nenhuma reconstrução foi necessária — e reescrever por cima via bash teria arriscado re-truncar. Lição reforçada: para verificar integridade de arquivo nesta pasta, usar Read/Grep diretos, não bash/`git show`. (2026-07-01)


- [ ] `compêndios/ia/inteligencia-artificial.html` — verificação visual no Chrome pendente (Claude-in-Chrome não navega de chrome://newtab para file://; verificação estrutural OK: 442 linhas, sidebar, progress bar, acentuação PT-BR, sem artefatos) (2026-06-30; reconfirmado ainda pendente em 2026-07-01 — entrada duplicada removida abaixo em 2026-07-03, consolidada aqui)

- [x] `compêndios/medicina/cardiologia-anatomia.html` — verificação visual confirmada no Chrome via surge.sh: pranchas Gray's Anatomy renderizando corretamente sem filtro, layout OK (2026-06-30)
- [ ] `compêndios/medicina/` — criar compêndios futuros de cardiologia: fisiologia cardíaca, valvopatias, IC, HAS, ECG, coronariopatias, arritmias (2026-06-30; semiologia cardíaca criada em 2026-08-05, ver `cardiologia-semiologia.html` — removida desta lista)
- [ ] `compêndios/medicina/cardiologia-semiologia.html` — verificação visual no Chrome PENDENTE (verificação bash-equivalente via Grep OK: fecha `</body></html>`, 492 linhas, sem `const const`/artefatos/placeholder, 114 acentos PT-BR, 16 seções = 16 links de sidebar). Compêndio de semiologia cardíaca (subskill compendio-medicina-clinica), em português (fontes: Porto, Bates-PT, Diretriz SBC de HAS). (2026-08-05)

- [ ] `compêndios/investimentos/fundamentos-investimentos.html` — adicionar cross-links para futuros compêndios de economia quando criados: inflação, juros reais, política monetária do Banco Central (conceitos mencionados inline sem material dedicado no projeto) (2026-06-22; caminho corrigido em 2026-07-03 — o arquivo está em `compêndios/investimentos/`, não em `investimentos/` na raiz, que hoje é uma pasta vazia)
- [ ] `compêndios/investimentos/fundamentos-investimentos.html` — conceitos mencionados por nome sem mecanismo desenvolvido que podem virar seções futuras: *factor zoo* (proliferação de fatores em Fama-French), paradoxo de Grossman-Stiglitz (limites da eficiência), limites da arbitragem (Shleifer-Vishny) (2026-06-22; caminho corrigido em 2026-07-03)

- [ ] `compêndios/medicina/mecanismos/imunologia/celulas-sistema-imune.html`, `imunidade-inata.html`, `hipersensibilidade.html`, `linfocitos-t-diferenciacao.html`, `mhc-apresentacao-antigenica.html` — editados na atualização em massa de 2026-07-01 sem backup prévio em `_archive/` (correções pontuais sob pressão de tempo, não reescrita ≥30% isolada). Se necessário reconstituir o estado anterior, usar histórico do Git. Caminho atualizado em 2026-07-10 (migrado de `medicina/imunologia/` para `compêndios/medicina/mecanismos/imunologia/`, desmembramento de Base de estudos). (2026-07-01)
- [ ] `compêndios/investimentos/fundamentos-investimentos.html` — único compêndio ativo sem `td-label` nas `.data-table` (padrão já presente em 22 outros arquivos). Reavaliado (2026-07-01): `.data-table`+`td-label` documentado como padrão canônico em `_docs/template-v2-spec.md` (seção "Elementos visuais") para materiais novos/reabertos — não retroaplicar só por isso (ver "Escopo de melhorias — não retroagir"). Corrigir apenas se este arquivo for reaberto por outro motivo.
- [ ] Diversos compêndios de imunologia/farmacologia/fisiopatologia atualizados nesta sessão (ver Resolvida) sem pranchas Gray's/OpenStax adicionadas — conteúdo predominantemente molecular/farmacológico sem imagem CC apropriada encontrada; reavaliar se novo material visual (ex. diagramas de via) ficar disponível (2026-07-01)

- [ ] `compêndios/ia/inteligencia-artificial.html` — dívida de conteúdo até então não registrada em DEBT.md, encontrada nesta sessão ao ler `compendio-ia/SKILL.md`: a própria subskill descreve o arquivo como "rascunho experimental... gerado quando a skill ainda era 'mapa orientador' — conteúdo raso para o novo propósito de compêndio de área" e sinaliza "reescrita com profundidade real" como pendência. A atualização em massa de 2026-07-01 corrigiu CSS/JS/tabelas, não a profundidade do conteúdo. Reescrever quando o usuário quiser retomar IA como compêndio de área — é geração de conteúdo (1ª reescrita substancial), portanto sessão em Opus + Esforço Alto/Extra por regra de pré-geração. (2026-07-03)

---

## Resolvida

- [x] **`index.html` (catálogo raiz) — card de `compêndios/medicina/infectologia-fundamentos.html` — RESOLVIDO (2026-08-05).** Adicionado nas duas seções onde os demais compêndios de medicina aparecem (listagem geral e tab-atlas), replicando o padrão de card já usado para hematologia-hemograma-anemias.html. Corrigido na mesma sessão de criação do compêndio — diferente do gap pré-existente de `cardiologia-semiologia.html` (ainda pendente, ver acima), que ficou faltando de uma sessão anterior.

- [x] **`compêndios/medicina/cardiologia-semiologia.html` — pranchas de imagem — RESOLVIDO (2026-08-05).** Usuário conectou o Claude in Chrome; `web_fetch` seguia bloqueado para `wikimedia.org`, e `javascript_tool` com `fetch()`+querystring foi bloqueado por filtro de segurança do Chrome MCP — contornado navegando a páginas `File:` normais (`get_page_text`) + construindo URL direta via hash MD5 do nome do arquivo (calculado localmente via Python/bash, não é fetch) + `navigate`/`screenshot` para confirmar carregamento. Duas pranchas adicionadas: Fig. 1 "Heart sounds auscultation areas" (domínio público, derivado de Gray's Anatomy/Gray112 + Auskultation.jpg) em "Focos de ausculta"; Fig. 2 "Wiggers Diagram" (CC BY-SA 2.5, DanielChangMD/DestinyQx/Xavax) em "Bulhas cardíacas normais", cross-linkada também em "Pulso venoso jugular" (mesma figura documenta ondas a/c/v). Método de sourcing de imagem documentado em `template-v2-spec.md`, seção "Fontes de imagens", para reuso em compêndios futuros.

- [x] **Projeto Cowork "Línguas" — status de domínio — RESOLVIDO (2026-07-15).** Pendência desde 2026-07-03 (recomendação original de virar 5º modo de "Base de estudos"), reaberta em 2026-07-10 quando o desmembramento de Base de estudos em Compêndios/Sistemas/Provas mudou a pergunta para "Línguas vira 4º domínio-irmão?" — teste empírico de 2026-07-10 já apontava resposta negativa (CSS/tema totalmente próprios, sem a máquina técnica compartilhada dos demais). Usuário confirmou nesta sessão, via AskUserQuestion, a recomendação: **manter como está** — Línguas continua roteado pela raiz do Cowork como hoje, sem virar domínio-irmão formal. Não fechar de novo salvo mudança real na "máquina técnica" de Línguas.

- [x] **Divergência normativa — Painel de dependências no arquivo-pai — RESOLVIDA (2026-07-03, autocrítica de sessão):** `template-v2-spec.md` marcava o painel como obrigatório sem exceção, mas a lista de 11 seções obrigatórias do arquivo-pai em `compendio/SKILL.md` ("Estrutura do arquivo-pai") não o incluía — causa raiz de `medicina.html` e `fisica.html` terem nascido sem ele. Corrigido: `compendio/SKILL.md` ganhou o painel como item 3 da lista (entre pergunta motivadora e "o que a área estuda"), com nota explícita de que as colunas podem ficar vazias num arquivo-pai partida-zero; checklist de fechamento também ganhou o item correspondente. `instrucoes-projeto.txt` (seção Pré-geração, regra bidirecional compêndio-irmão↔spec) foi estendido para cobrir também o caso geral "lista de estrutura de uma skill-pai desatualizada em relação a um item obrigatório do spec" — vale para qualquer skill-pai, não só compendio-medicina. `fisica.html` continua sem o painel (ver Pendente) — só `medicina.html` foi corrigido nesta sessão, por escolha do usuário.

- [x] **Organização hierárquica — pasta Cowork "Estudo para prova"** — a pedido do usuário ("organizar os arquivos na pasta de forma hierárquica"), investigada por completo: os 4 HTMLs migrados (`agressao-defesa-RA3.html`→`agressao-defesa.html`, `PAB2/epidemiologia.html`→`epidemiologia.html`, `Pesquisa-em-Saude/guia.html`→`pesquisa-em-saude.html`, `Semiologia/HP3.html`→`semiologia.html`) e os 2 caches confirmados idênticos ao conteúdo em `provas/medicina/` (comparação de início/fim de cada arquivo + contagem de linhas dos caches). Resto da pasta (`_recursos/` com skill `autocritica-sessao` divergente, `instrucoes-projeto.md`/`instrucoes-modulos.md`/`template-base.html` próprios, um zip de nome aleatório `ziylVWsg` contendo cópia da mesma skill, e um `autocritica-sessao.skill` de 0 bytes) confirmado 100% obsoleto — substituído pela skill `prova`/`prova-medicina` deste repositório. Usuário confirmou via AskUserQuestion remoção completa da pasta; `rm -rf` executado mas bloqueado por lock de sincronização do OneDrive na sessão original — **deleção manual confirmada concluída pelo usuário: verificado nesta sessão (autocrítica retroativa) que a pasta `Estudo para prova/` não existe mais na raiz do Cowork.** Demais pastas Cowork (Anamnese e Exame Físico, Línguas, Investimentos, Trabalhos genéricos, Keidi) avaliadas nesta primeira parte da sessão e inicialmente confirmadas como projetos de propósito genuinamente distinto, sem overlap com as skills de Base de estudos. **Nota (mesma sessão, parte 2 — "lente entre-projetos"): essa conclusão sobre "Línguas" foi revisada** — ao ser perguntado especificamente sobre fusão de projetos, o Claude reavaliou e identificou Línguas (estudo pessoal recorrente) como candidato real a skill-pai/subskill nova, distinto dos demais (Anamnese, Investimentos, Trabalhos genéricos, Keidi seguem confirmados sem overlap). Decisão sobre Línguas ainda não tomada pelo usuário — ver item em Pendente. Regra geral registrada em `instrucoes-projeto.txt`, seção "Escopo do repositório — evitar duplicação em pastas Cowork paralelas", para que material futuro do escopo das quatro skills nasça direto em Base de estudos. (2026-07-03; nota de correção adicionada em 2026-07-03 por sessão de autocrítica retroativa)

- [x] Auditoria geral do projeto (instruções, skills, memória) — sessão dedicada a pedido do usuário ("revise todo o projeto... sem pontas soltas"), 2026-07-03. Itens corrigidos na mesma sessão:
  - `_docs/project_estado.md` criado (histórico e pendências de mecanismo/medicina) — estava referenciado por `mecanismo/SKILL.md` e `instrucoes-projeto.txt` desde 2026-07-02 mas nunca existia no disco.
  - `_docs/project_mapa_materiais.md` reescrito — escopo restrito a `medicina/` (mecanismo); a versão anterior misturava as três skills e citava caminhos mortos (`ia/fundamentos/*`, `ferramentas/*`, `investimentos/` sem prefixo `compêndios/`).
  - `_docs/instrucoes-projeto-ia.txt` movido para `_acervo/docs-legado/instrucoes-projeto-ia.txt` (legado pré-skill, workflow e caminhos obsoletos, duplicava o procedimento de Anki fora do lugar certo). Original removido do disco após cópia confirmada; nota adicionada em `_acervo/LEIA-ME.md`.
  - `_docs/MIGRATION.md` movido para `_acervo/docs-legado/MIGRATION.md` — o rastreamento de migração ao Template v2 já tinha cumprido seu papel (todo o acervo ativo migrado em 2026-07-01) e o arquivo tinha parado de ser atualizado, com status incorretos. Referência removida de `mecanismo/SKILL.md` (fluxo de trabalho passo 7 e checklist).
  - `compêndios/compendio_mapa.txt` — todos os caminhos corrigidos para incluir o prefixo `compêndios/` (antes ambíguos com a pasta raiz `investimentos/`, hoje vazia).
  - `instrucoes-projeto.txt` — contagem de `medicina/imunologia/` corrigida de "10" para "13 compêndios ativos" e `medicina/fisiologia/` de "1" para "2" (contagem real de arquivos no disco).
  - `mecanismo-medicina-basica/SKILL.md` — `ciclo-cardiaco.html` estava ausente da lista de arquivos de `medicina/fisiologia/` e dos cross-links prioritários; adicionado.
  - `compendio-fisica/SKILL.md` e `compendio-filosofia/SKILL.md` — seção "Material existente no projeto" desatualizada (dizia "nenhum compêndio criado" quando `fisica.html` e `epistemologia.html` já existiam desde 2026-07-01/02); corrigida.
  - Comentário órfão removido de `medicina/fisiopatologia/choque-circulatorio/choque-circulatorio.html` (linha ~262, resíduo de edição de 2026-07-01).
  - Convenção de `_archive/` aninhado por subpasta em `medicina/` (vs. `_archive/` único na raiz usado por `compêndios/`) documentada explicitamente em `_docs/operacoes-bash.md`, seção "Arquivamento", como coexistência aceita — não é mais uma divergência não-documentada.
  - `.gitignore` — `*.lnk` adicionado (ver item pendente sobre `Claude.lnk`).
  - `compêndios/ia/inteligencia-artificial.html` — dívida de conteúdo (raso para o propósito atual de compêndio de área) que só existia dentro de `compendio-ia/SKILL.md`, nunca registrada em `DEBT.md`; adicionada à seção Pendente.
  - Memória (`project_base_estudos.md` e demais arquivos de memória) conferida — já refletia a maior parte destes achados de uma auditoria anterior (2026-07-03); atualizada para marcar os itens acima como resolvidos em vez de pendentes.
  Confirmado com o usuário (via AskUserQuestion) e removidos na mesma sessão: `_config/manifest.json` + `_config/icon-192.png` + `_config/icon-512.png` (obsoletos, não referenciados por `index.html` nem por nenhum `.bat` — confirmado por grep antes de apagar); `Claude.lnk` (arquivo pessoal do usuário na raiz do repo); pasta raiz `investimentos/` (vazia).

- [x] **Dicionário ao vivo (`#dict-panel`) promovido de piloto a padrão** — o usuário reverteu, na mesma sessão, a decisão de manter o `#dict-panel` (busca ao vivo por duplo-clique via dictionaryapi.dev) isolado no `cardiologia-anatomia.html`, e pediu para tê-lo. Promovido: CSS `#dict-panel`/`.dict-*` + JS (IIFE) adicionados a `fisica.html` e `medicina.html`; registrado no `template-v2-spec.md` (seção "Idioma e tooltips de tradução", CSS+JS canônicos; nota do App shell atualizada). Adaptação em relação ao original do cardiologia: o guard do duplo-clique passou de `closest('.term')` para `closest('.term, .term-rich')`, porque física/medicina usam `.term-rich` sozinho (sem a classe `.term`) — sem isso, clicar num termo curado abriria o painel indevidamente. Escopo: só compêndios em inglês (a API cobre só inglês); NÃO aplicar ao acervo PT. Verificação Grep/Read: `#dict-panel` (CSS) + `dictionaryapi.dev` (JS) presentes nos dois, IIFE fecha `})();` antes de `</script>`, ambos fecham `</body></html>`. Verificação visual via surge PENDENTE (testar duplo-clique numa palavra comum e hover num termo curado). (2026-07-02)
- [x] **Tooltip de tradução enriquecido (`.term-rich`) promovido a padrão** — o usuário notou que o tooltip rico do `cardiologia-anatomia.html` (tradução PT em negrito + glosa explicativa multilinha) tinha ficado restrito àquele arquivo, enquanto `fisica.html` e `medicina.html` usavam só o `.term` simples (uma linha). Decisão (via AskUserQuestion): padronizar o `.term-rich` (versão autossuficiente, sem combinar com `.term`) no `template-v2-spec.md` para compêndios em idioma estrangeiro, E aplicar já em `fisica.html` e `medicina.html` (medicina em edição nesta sessão; física é o mapa irmão em inglês). Não retroagir ao acervo PT. Aplicado: CSS `.term-rich` + conversão de 18 tooltips em medicina e 14 em física; `aria-label` adicionado às 2 `.data-table` de física (convenção também registrada no spec). O painel de dicionário ao vivo `#dict-panel` (dictionaryapi.dev) foi inicialmente mantido isolado nesta sessão, mas o usuário **reverteu logo em seguida** e pediu o dicionário — ver entrada abaixo (promovido a padrão). Verificação Grep/Read: 0 `.term`/data-pt órfãos restantes nos dois arquivos, tips bem-formados, ambos fecham `</body></html>`. Cor do `td-label`: o usuário decidiu seguir o `cardiologia-anatomia.html` (âmbar `--ac` + `white-space:nowrap`). Spec atualizado para `--ac`, e `fisica.html`/`medicina.html` alinhados; cardiologia agora está em conformidade. Acervo PT (que usa `--text`) permanece grandfathered — não retroagir. (2026-07-02)
- [x] `_docs/skills-export/*.skill` (compendio-medicina-basica, compendio-medicina-clinica, mecanismo-medicina-basica, mecanismo-medicina-clinica) + `instrucoes-projeto.txt` (seções "Busca de literatura biomédica (MCP)" e "Subagentes para auditoria em lote") + `git push` — usuário confirmou execução local (`empacotar_skills.py` via duplo clique, reinstalação via Settings > Capabilities > Skills, e `push.bat`). (2026-07-01)
- [x] `.git` isolado do OneDrive — causa raiz de vários incidentes acima (`.git/index.lock` preso, `git show`/leituras desatualizadas pelo cache do OneDrive) eliminada: `.git` movido para `%LOCALAPPDATA%\git-repos\estudo-conceitos.git` (fora de qualquer pasta sincronizada), com arquivo `.git` de referência (`gitdir: ...`) no lugar da pasta original na raiz do projeto. Script `_config/isolar-git-onedrive.bat` criado para a migração (usa `robocopy` com retry + verificação de integridade de HEAD/objects/refs antes de apagar o original — `move` direto falhou 2x com "sistema não pode encontrar o arquivo especificado", provável interação com sincronização ativa do OneDrive). Migração confirmada: `git status`/`git log -3` no terminal nativo mostram histórico intacto (mesmo HEAD `16cab15`, branch `main` up to date com `origin/main`). Efeito colateral aceito: o bash do sandbox desta sessão não enxerga mais o gitdir externo (fora de qualquer pasta mapeada) — comandos git a partir de agora dependem do terminal Windows nativo ou `push.bat`, verificação de conteúdo continua via Read/Grep. O working tree (HTMLs, docs) continua sincronizado pelo OneDrive normalmente, sem mudança. (2026-07-01)
- [x] `_docs/skills-export/autocritica-sessao/SKILL.md` — truncamento antigo encontrado (cortava em "Reinstalar ski", já presente desde ao menos o commit `a9fc38c`, sobrevivendo a 2 commits sem ser detectado). Reconstruído a partir de `git show a9fc38c` (versão íntegra mais antiga) + conteúdo atual de Passo 3. Regra obsoleta sobre heredoc python3 substituída pela regra vigente de Causa/Consequência no Passo 2.5. (2026-07-01)
- [x] Diversos arquivos truncados no working directory no início da sessão de 2026-07-01 (DEBT.md, instrucoes-projeto.txt, template-v2-spec.md, compendio_estado.txt, compendio_mapa.txt, cardiologia-anatomia.html, 8x `.skill`) — restaurados via `git show HEAD:"<arquivo>" > "<arquivo>"` sem perda (nada estava commitado no estado truncado). (2026-07-01)
- [x] Deleções manuais pendentes — usuário apagou os 6 arquivos legados/resíduo confirmados via Read direto (não existem mais): `instrucoes-atlas.txt` (raiz), `_docs/instrucoes-atlas.txt`, `.claude/` (pasta inteira), `medicina/fisiopatologia/doencas-circulatorias.html.bak`, `medicina/microbiologia/micologia-medica.html.bak`, `medicina/microbiologia/virologia-geral.html.bak`. Conteúdo do legado Atlas segue preservado em `_acervo/docs-legado/`. Pendente ainda: rodar `git add -A`/commit/`push.bat` para refletir as remoções no repositório remoto (bash do sandbox ainda mostra cache desatualizado do OneDrive, não usado para esta verificação). (2026-07-01)
- [x] Legado Atlas (`instrucoes-atlas.txt` raiz e `_docs/`, `.claude/skills/atlas/`, `.claude/skills/mecanismo/`) — conteúdo copiado integralmente para `_acervo/docs-legado/` (instrucoes-atlas-raiz.txt, instrucoes-atlas-docs.txt, claude-skills-legado/atlas-SKILL.md, claude-skills-legado/mecanismo-SKILL.md), com nota em `_acervo/LEIA-ME.md`. Confirmação do usuário: "não há mais atlas, é compêndio". Remoção dos originais segue pendente por lock do OneDrive (ver Pendente). (2026-07-01)
- [x] `compêndios/medicina/cardiologia-anatomia.html` — piloto de dicionário interativo (`.term-rich` + `#dict-panel`/dictionaryapi.dev) reavaliado: decisão é manter como piloto isolado, sem expandir para os demais termos do arquivo nem replicar em outros compêndios — melhoria pontual não vira padrão retroativo (ver `_docs/instrucoes-projeto.txt` seção "Escopo de melhorias — não retroagir"). (2026-07-01)
- [x] `.data-table`/`td-label` documentado como padrão canônico em `_docs/template-v2-spec.md` (seção "Elementos visuais") — já era o padrão de fato em 22 dos 23 compêndios ativos, faltava só registrar na fonte de verdade. (2026-07-01)
- [x] `index.html` — reavaliado: conteúdo do catálogo já estava atualizado (todos os compêndios/mecanismos ativos presentes), item antigo estava desatualizado. Corrigido de fato: arquivo tinha ~4KB de bytes nulos (`\x00`) no final (corrupção antiga, harmless para navegadores mas suja) — removidos; data do subtítulo atualizada para 2026-07-01; adicionado suporte a PWA (`<link rel="manifest">`, `theme-color`, ícone) para instalação como app. Backup do estado anterior em `_archive/index_2026-07-01.html`. (2026-07-01)
- [x] App instalável (PWA) — criado `manifest.json` + `assets/icons/` (3 ícones gerados via Python/PIL) + `.nojekyll` na raiz, para o Chrome oferecer "Instalar app" a partir do GitHub Pages já publicado. Ver `_docs/template-v2-spec.md` seção "App shell (PWA)". (2026-07-01)
- [x] Link de retorno ao índice raiz (`#s-home` / `.home-link`) ausente em todos os 26 HTMLs ativos de `compêndios/` e `medicina/` — usuário reportou ficar "preso" ao abrir um compêndio a partir do index (sem botão de voltar do navegador disponível no app shell usado). Corrigido: `template-v2-spec.md` ganhou seção canônica "Link de retorno ao índice (`#s-home`)"; aplicado o link em 25/26 arquivos (`medicina/imunologia/index.html` já tinha breadcrumb próprio para o índice local — fora do escopo). Dois arquivos com sidebar em variante não-padrão (`medicina/farmacologia/hipertensao_sraa_anti-hipertensivos.html` — sidebar fixa sem toggle; `medicina/imunologia/vacinas-imunidade-protetora.html` — toggle-btn fora do wrapper `#s-toggle`) receberam adaptação equivalente em vez do padrão exato. (2026-07-01)
- [x] **Incidente de truncamento durante a correção acima** — ao ler os 25 arquivos via bash para aplicar o patch, vários (`compêndios/ia/inteligencia-artificial.html` e outros) tiveram o conteúdo lido incompleto (corte no meio do JS final, antes de `</script></body></html>`) devido ao cache de leitura do OneDrive já documentado neste projeto — e a escrita subsequente teria persistido essa versão truncada no disco, deletando o fechamento de ~20 arquivos. Detectado via `git diff --stat` (mostrando dezenas de linhas deletadas onde deveria haver só inserção) antes de finalizar a sessão. Corrigido reconstruindo cada arquivo a partir do conteúdo íntegro em `git show HEAD:<arquivo>` (confirmado completo) + reaplicação do patch, com verificação final via `Read` direto (não bash) e `git diff` limpo. Nenhuma perda de conteúdo no resultado final. Reforça a regra já existente de não confiar em leitura via bash logo após escrita em pasta OneDrive — idealmente confirmar com `git diff --stat` (não só `wc -l`) após qualquer escrita em lote nesta pasta, já que `wc -l` sozinho não pega truncamento no meio do arquivo tão bem quanto uma revisão de diff. (2026-07-01)
- [x] `medicina/fisiologia/ciclo-cardiaco.html` — encontrado truncado no working directory (598 linhas, cortado no meio de uma frase/byte UTF-8, sem tags de fechamento) — edição incompleta e não commitada de sessão anterior, anterior à correção acima. Restaurado a partir do último commit no Git (804 linhas, íntegro, termina em `</html>`) e o link `#s-home` foi aplicado sobre essa versão restaurada. Qualquer conteúdo da edição incompleta além do que já estava commitado foi perdido — não havia como recuperá-lo (não estava em nenhum commit nem archive mais completo). (2026-07-01)
- [x] Atualização em massa ao modelo atual (2026-07-01) — todos os compêndios pré-existentes revisados: sidebar scroll-based (substituindo `IntersectionObserver` para `.active`), tabelas convertidas para `.data-table`, chip de "Última revisão" atualizado. Tier A (imunologia × 9 + hipertensão-SRAA + trombose-hemostasia): tratamento completo (CSS+tabelas+JS+pranchas Gray's/OpenStax onde aplicável). Tier B (choque-circulatorio): completo com pranchas. Tier C (virologia, micologia, antifúngicos, antiagregantes/anticoagulantes, doenças circulatórias, citocinas, Th1/Th2/Th17, vacinas): tabelas + JS, sem pranchas novas (conteúdo molecular/farmacológico). Tier D (compêndios de área IA e investimentos): JS de sidebar corrigido; investimentos com 16 tabelas convertidas. `medicina/imunologia/index.html` e `_acervo/automoveis` avaliados e mantidos fora do escopo (templates/formatos distintos — ver notas). (2026-07-01)
- [x] `_acervo/automoveis/` — avaliado: mantido como referência em `_acervo/`, não promovido a compêndio de área (pesquisas pontuais, não campo de estudo sistemático) (2026-07-01)
- [x] `investimentos/fundamentos-investimentos.html` — 9 itens de dívida técnica registrados e resolvidos na sessão de 2026-06-22: célula Tabela 12 truncada; referência Yearbook 2024 ausente; Tabela 15B sem fonte; numeração de tabelas com colisão (1–16 agora sequencial); #bazin promovido a section; ordem CAPM → objetivo-pessoal; fórmula de Gordon formalizada; colchão de liquidez desenvolvido; pergunta motivadora reformulada. (2026-06-22)
- [x] `medicina/fisiologia/ciclo-cardiaco.html` — verificação visual confirmada via preview do Cowork (2026-06-30)
- [x] `compêndios/medicina/cardiologia-anatomia.html` — compêndio de anatomia cardíaca criado: câmaras, valvas, grandes vasos, coronárias, sistema de condução, pericárdio, topografia, parede cardíaca (2026-06-30)
- [x] `compêndios/medicina/cardiologia-anatomia.html` — verificação visual confirmada no Chrome via surge.sh: pranchas Gray's Anatomy renderizando corretamente sem filtro, layout OK (2026-06-30)
- [x] `atlas-ia` (skill) — empacotada com emp